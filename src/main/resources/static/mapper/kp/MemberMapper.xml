<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.loven.mapper.MemberMapper">

    <insert id="memberJoin" parameterType="UserVo">
        insert into User(id, pw, name, nick, email, reg_date, login_type, company_name)
        values (#{id}, #{pw}, #{name}, #{nick}, #{email}, sysdate(), 's', #{company_name})
    </insert>
    <insert id="kakaoLogin" parameterType="UserVo">
        insert into User(id, name, nick, email, reg_date, login_type)
        values (#{id}, #{name}, #{nick}, #{email}, sysdate(), 'k')
    </insert>

</mapper>